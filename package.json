{
  "name": "met-qua-di",
  "version": "1.0.0",
  "scripts": {
    "start": "parcel html/index.html",
    "build": "parcel build html/*.html --dist-dir build && copy public\\OneSignalSDKWorker.js build\\"
  },
  "devDependencies": {
    "parcel": "^2.13.0"
  }
}
